package constants

const ACTION_APP_AWAKE = "ela.system.action.APP_AWAKE" // called when an app start running
const ACTION_APP_SLEEP = "ela.system.action.APP_SLEEP" // called when app stop running

// CONNECTION CONFIG
const PORT = 9000
const TIMEOUT = 5      // transmit timeout
const RECONNECT = true // true if enable reconnection
