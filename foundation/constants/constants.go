package constants

// CONNECTION CONFIG
const PORT = 80
const TIMEOUT = 5      // transmit timeout
const RECONNECT = true // true if enable reconnection
const PACKAGE_EXT = "box"
const CUSTOMINSLLER = "custominstaller"
