Scripts to perform various build, install, analysis, etc operations.

NOTE: native windows is not tested at the moment

setup.sh 
- use to setup development environment. download golang, toolchains, dependeng projects etc...

build.sh 
- build, generate package and binaries of Elabox system
- to use:
    cd <scripts directory>
    ./build.sh -o <can linux/windows/macos> -a <can be amd64/arm64/386>

install.sh
- install the system based from package

run.sh
- build install and run specific package

uninstall.sh
- use to uninstall the system

dlinstall.sh
- use to download from storage server and install the system

upload.sh
- use to upload the package and installer to storage server

imager.sh
- create and compress the specific usb storage as image